# Prerender Server Configuration
# Copy this file to .env and adjust as needed

# Server port (xCloud will set this automatically)
PORT=3000

# Cache settings
MEMORY_CACHE=1
CACHE_MAXSIZE=1000
CACHE_TTL=86400

# Set to 0 to disable cache (for debugging)
# MEMORY_CACHE=0
