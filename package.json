{
  "name": "prerender-server",
  "version": "2.0.0",
  "description": "Self-hosted prerender server for SPA SEO. Renders JavaScript-heavy pages and serves static HTML to search engine bots.",
  "main": "server.js",
  "scripts": {
    "start": "LD_LIBRARY_PATH=$HOME/chrome-libs/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH node server.js",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "express": "^4.21.0",
    "puppeteer": "^24.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
